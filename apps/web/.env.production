# Production build overrides — loaded by `vite build` (mode: production).
# Takes priority over .env. VITE_CLERK_PUBLISHABLE_KEY is provided by the
# build environment (local .env for manual deploys, dashboard env for CI).
VITE_API_URL=
VITE_GUEST_MODE=true
